box: wercker/nodejs
build:
  steps:
    - npm-install

    - script:
      name: update webdriver
      code: webdriver-manager update

    - npm-test

    - script:
        name: echo nodejs information
        code: |
          echo "node version $(node -v) running"
          echo "npm version $(npm -v) running"
